# === 所有資料目錄（本地保存，不上 git，各機各自重抓）===
data/
bwibbu_*.json

# === Python ===
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# === 系统 ===
.DS_Store
Thumbs.db
desktop.ini

# === 中间产物 (每日重生成, 但网页要用的 *_data.json 保留见下) ===
/tmp/

# === 保留 (强制加回网页需要的成果 JSON) ===
!stocks_data.json
!dashboard_data.json
!gate_data.json
!gatelight_data.json
!ls_backtest_data.json
!chips_data.json
!chips_backtest_data.json

# 私人记忆/排程 (不上公开 repo)
.claude/
update.log

# finmind(台指期,7MB) + chips(法人筹码) 共享上传, 避免各机重抓触 FinMind 额度
# 已在顶部用 /data/* + !/data/finmind/ + !/data/chips/ 放行整个子目录
