# 🚀 懶人包：一次設定好（Windows）

跟著做，約 10 分鐘設定 + 等 1-2 小時抓資料，之後全自動。

---

## 步驟 0：先裝兩個軟體（各按 next 到底就好）

1. **Python 3**：https://www.python.org/downloads/
   ⚠️ **安裝第一頁一定要勾選「Add Python to PATH」**（很重要，忘了勾要重裝）
2. **Git**：https://git-scm.com/download/win

---

## 步驟 1：下載專案

按「開始」搜尋 **cmd**，打開命令提示字元，複製貼上這行按 Enter：

```
cd %USERPROFILE% && git clone https://github.com/Durant0509/tw-stock.git && cd tw-stock
```

---

## 步驟 2：一鍵設定

在同一個 cmd 視窗，打：

```
setup_windows.bat
```

它會自動：
- ✅ 檢查 Python / Git
- ✅ 全抓三年資料（約 1-2 小時，資料只存本機，**不會上傳**）
- ✅ 建立每天 15:30 和 21:30 的自動更新排程

**掛著等它跑完就好。**

---

## 步驟 3：完成！以後怎麼用

- 電腦**不關機**，它每交易日自動更新
- 隨時打開這個網址看最新狀況（手機也行，建議加書籤）：

  ### 👉 https://durant0509.github.io/tw-stock/warroom.html

- 網頁最下面有「📚 新手學習總結」，看不懂數字就往下拉

---

## ❓ 常見問題

**Q：資料會一直上傳 GitHub 塞爆嗎？**
不會。8GB 歷史資料只存你本機 `data\` 資料夾。GitHub 上只有程式+網頁(約1MB)，每天更新也只傳那幾個小檔。

**Q：第一次自動更新說要登入 GitHub？**
對，第一次 push 會要帳號 + token。輸入你的 GitHub 帳號 `Durant0509` 和一組 token（去 GitHub → Settings → Developer settings → Personal access tokens 產生一個，勾 repo 權限）。只需設定一次，之後記住。

**Q：電腦晚上關機了怎麼辦？**
排程會跳過。下次開機不會自動補跑，但你手動在 tw-stock 資料夾打 `python update.py` 就能補更新。

**Q：想確認有沒有正常跑？**
看 tw-stock 資料夾裡的 `update.log`，有每次執行紀錄。

**Q：週末會跑嗎？**
排程會觸發，但程式judged是週末就跳過抓資料（台股沒開盤沒新資料），只確保網頁在線。
